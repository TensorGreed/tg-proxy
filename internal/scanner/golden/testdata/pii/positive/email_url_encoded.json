{
  "comment": "GRADUATED from adversarial: Email URL-encoded in a query string. Fix would require URL-decoding bodies before scanning (or matching %40 as an alternative for @).",
  "findings": [
    {
      "type": "pii.email",
      "match": "alice%40example.com"
    }
  ]
}