{
  "comment": "GRADUATED from adversarial: email with spaces around @. The pipeline-level whitespace-stitch pass identifies the token-shaped region, removes internal whitespace, runs scanners on the stitched view, and maps offsets back to the original buffer.",
  "findings": [
    {"type": "pii.email", "match": "alice @ example.com"}
  ]
}
