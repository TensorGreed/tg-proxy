{
  "comment": "GRADUATED from adversarial: email using a full-width @ (U+FF20) instead of ASCII @. NFKC normalization in Pipeline.Scan handles this — the dual pass scans the normalized form and maps offsets back so the redactor still rewrites the original Unicode-containing span.",
  "findings": [
    {"type": "pii.email", "match": "alice＠example.com"}
  ]
}
