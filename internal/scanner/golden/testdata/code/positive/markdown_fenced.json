{
  "comment": "GRADUATED from adversarial: Python function inside a markdown ```python fence. The current regex sees it (fences are just text to the matcher), so we expect this to graduate to positive/ soon. If it does, the runner will tell us.",
  "findings": [
    {
      "type": "code.python_def",
      "match": "def greet("
    }
  ]
}