{
  "comment": "Canonical AWS doc fixtures. The secret access key is intentionally not detected today (40-char base64 has too many false positives without context).",
  "findings": [
    {"type": "secret.aws_access_key_id", "match": "AKIAIOSFODNN7EXAMPLE"}
  ]
}
