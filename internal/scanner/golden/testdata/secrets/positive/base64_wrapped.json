{
  "comment": "GRADUATED from adversarial: AKIAIOSFODNN7EXAMPLE base64-encoded as QUtJQUlPU0ZPRE5ON0VYQU1QTEU=. Pipeline.Scan's base64 pre-pass finds the candidate run, decodes it to mostly-printable text, scans the decoded form, and maps offsets back so the redactor still rewrites the original base64 span.",
  "findings": [
    {"type": "secret.aws_access_key_id", "match": "QUtJQUlPU0ZPRE5ON0VYQU1QTEU="}
  ]
}
