{
  "comment": "AKIAIOSFODNN7EXAMPLE base64-encoded as QUtJQUlPU0ZPRE5ON0VYQU1QTEU=. Fix would need a 'try-base64-decode-then-scan' pre-pass on suspicious-looking blobs (or a dedicated heuristic that base64-decodes high-entropy fixed-length tokens).",
  "ideal_findings": [
    {"type": "secret.aws_access_key_id", "match": "QUtJQUlPU0ZPRE5ON0VYQU1QTEU="}
  ]
}
