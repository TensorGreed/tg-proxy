{
  "comment": "AWS access key broken by a stray space or newline. Common artifact when a key is line-wrapped in pasted documentation or email. Fix would need a 'stitch and validate' pass: collapse whitespace inside candidate regions before applying the regex.",
  "ideal_findings": [
    {"type": "secret.aws_access_key_id", "match": "AKIA IOSFODNN7EXAMPLE"}
  ]
}
