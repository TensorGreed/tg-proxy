The team rotated AWS credentials this quarter and updated the GitHub personal
access tokens used by CI. We discussed best practices around storing API keys:
never commit them to git, always use a secret manager, and prefer short-lived
JWTs over long-lived bearer tokens. The OpenAI integration was migrated from
api-key auth to OAuth where possible.
