{
  "comment": "URL-encoded UNION SELECT in a query string. Same fix as the URL-encoded email/secret cases — decode bodies before matching.",
  "ideal_findings": [
    {"type": "sqli.union_select", "match": "%55%4E%49%4F%4E%20%53%45%4C%45%43%54"}
  ]
}
